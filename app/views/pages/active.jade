doctype
html
	head
		meta(charset="utf-8")
		title #{title}
		include ../includes/head
	body
		h1.text-center #{title}
		.container
			.row
				.col-md-4.col-md-offset-4.text-center
					h2 #{inxt.t('active')}#{result}
					button.btn.btn-block.btn-default
						a(href="/") #{inxt.t('Go')}&nbsp;#{inxt.t('Home')}
		script